---
interface Props {
  width: string;
  height: string;
  className: string;
  id: string;
}
const { width, height, className, id } = Astro.props;
---

<svg
  class={className}
  id={id}
  width={width}
  height={height}
  viewBox="0 0 757 731"
  fill="none"
>
  <path d="M405 179.5L755.5 77.5L749 582.5L405.5 730.5" stroke="white"></path>
  <path d="M406 179.5L1 85L12 591.5L406 730.5" stroke="white"></path>
  <path d="M405.5 179.5V730" stroke="white"></path>
  <path d="M1.5 85L369.5 1L755.5 77.5" stroke="white"></path>
</svg>

<script>
  import anime from "animejs/lib/anime.es.js";
  const link = document.querySelectorAll(".link a");
  const svg = document.querySelectorAll("svg");
  // const links = Array.from(link).map((el) => el.id);
  // const dic = links.map((link, i) => ({
  //   link,
  //   i,
  // }));

  // console.log(dic[0]);

  link.forEach((el, i) => {
    el.addEventListener("mouseenter", () => {
      // console.log(dic[i].link);

      svg[i].classList.add("active");
      anime({
        targets: svg[i],
        scale: 1.2,
      });
    });
  });

  link.forEach((el, i) => {
    el.addEventListener("mouseleave", () => {
      svg[i].classList.remove("active");
      anime({
        targets: svg[i],
        scale: 1,
      });
    });
  });
</script>

<style>
  svg {
    /* fill: blue; */
  }

  svg.active {
    /* fill: blue; */
  }
</style>
